# Docker Infrastructure

This folder is for Docker-related deployment notes and helper files.

The MVP uses root-level `docker-compose.yml` with Caddy, Next.js, FastAPI, Celery, PostgreSQL, and Redis.
