# Use PostgreSQL as the primary database

We will use PostgreSQL as the system of record for users, grocery lists, stores, retailers, canonical products, product listings, product mappings, price history, and proposed plans. The domain is highly relational, and PostgreSQL also gives us a path to geospatial queries with PostGIS and semantic matching with pgvector.
