# Use Caddy as the reverse proxy

We will use Caddy as the MVP reverse proxy and HTTPS layer on the VPS. Caddy provides automatic TLS, simple configuration, and good Docker Compose ergonomics, which reduces operational overhead compared with Nginx for the initial deployment.
