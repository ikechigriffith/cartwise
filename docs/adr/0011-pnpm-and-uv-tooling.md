# Use pnpm and uv for project tooling

We will use pnpm for the TypeScript workspace and uv for the Python FastAPI app. pnpm gives efficient dependency management and strong monorepo support for web/mobile/shared packages, while uv provides fast, reproducible Python dependency installation for local development, Docker, and CI.
