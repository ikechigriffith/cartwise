# Use FastAPI for the backend

We will use FastAPI for the backend API instead of a TypeScript framework such as Fastify. Although this creates a polyglot stack with TypeScript for web/mobile and Python for backend/data work, the product's hardest problems are scraping, product normalization, price history, confidence scoring, and future semantic matching, where Python's data ecosystem provides stronger leverage.
