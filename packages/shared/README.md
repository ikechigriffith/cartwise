# Shared Package

Placeholder for shared generated API clients, constants, and cross-app documentation.

Because the backend is FastAPI/Python, shared TypeScript API types should be generated from the FastAPI OpenAPI schema rather than manually duplicated.
